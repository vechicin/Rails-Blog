<%= form_with model: [@user, @post, @comment] do |form| %>
  <div>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </div>

  <div>
  <%= form.submit "Post Comment" %>
</div>
<% end %>